# Agents 必读规则

所有 agents 的所有 session 在开始任务前必须先阅读本文件。

## 协作规则

1. 不要做额外内容，不要做兜底。即使可能出现逻辑 bug，也不要自行扩展或补救；编码错误、编译错误、引用错误等可以修。保持逻辑干净利落，问题也干净利落地暴露。
2. 必须使用简体中文和用户沟通。新建或修改的说明文件、规则文件、文档文件也必须使用简体中文。撰写 git commit 信息时也必须使用简体中文。
3. Unity 中暴露到 Inspector 的参数名称、说明、菜单名等，应尽量做成中文，方便用户直接理解。
4. 用户提出需求时，检查、阅读、定位可以直接做；只有要改代码、改资源、改配置时，才必须先用简体中文说明准备怎么作业、会改哪里、为什么这样做，并等待用户明确同意后再开始执行。
5. 提到代码中的功能、类、方法、变量、组件、资源类型时，尽量翻译成中文解释。不要只给代码名字；需要说明它的中文含义和用途。
6. Unity Inspector 参数改动要优先考虑用户实际拖拽方式。大部分新加或调整的 Inspector 参数，尤其涉及条件显示、校验、按钮、批量应用、共享配置、拖拽引用、数组/列表、多个参数组合时，应优先通过 `Assets/Editor` 下的自定义编辑器统一呈现中文字段名、中文提示和必要校验；不要只依赖运行时脚本里的 `InspectorName` / `Tooltip`。如果需求是“拖一个 GameObject，但要求它带某个组件”，运行时字段应保持为 `GameObject`，并通过自定义编辑器做中文提示和校验；不要仅仅把字段类型改成组件类型来解决。

## Encoding / 中文编码规则

1. 本项目大量使用中文文件名、中文类名、中文字段名、中文菜单名和中文文档内容，默认按 UTF-8 处理文本文件。
2. PowerShell 读取中文文本文件时必须显式使用 `Get-Content -Encoding UTF8`；不要直接使用未指定编码的 `Get-Content` 判断中文内容。
3. 修改或新建 `.cs`、`.md`、`.json`、`.shader`、`.asset` 等文本文件时，必须保持 UTF-8 编码，避免中文内容变成乱码。
4. 搜索文件内容优先使用 `rg`；需要查看具体中文文本时，再用 UTF-8 显式读取。

## 项目结构速览

这是一个 Unity 项目，项目根目录为 `C:\Users\neikuy\Desktop\Blue Sun`。

- `Assets/Scenes`：Unity 场景文件。
- `Assets/Scripts`：主要 C# 脚本。
- `Assets/Editor`：Unity 编辑器扩展脚本，只在编辑器中运行。
- `Assets/Resources`：通过 `Resources.Load` 等方式运行时加载的资源。
- `Assets/Shaders`：项目 Shader 文件。
- `Assets/资源`：主要美术、图标、贴图、场景素材等中文资源目录。
- `Assets/配音`：语音或音效相关资源。
- `Assets/音乐包`：音乐资源。
- `Assets/字体包`：字体资源。
- `Packages`：Unity 包管理配置。
- `ProjectSettings`：Unity 项目设置。
- `Library`、`Temp`、`obj`：Unity 或编译生成目录，通常不要手动修改。

## 作业前最小检查

1. 先读本文件。
2. 只围绕用户当前需求定位相关目录和文件。
3. 检查、阅读、定位可以直接做。
4. 需要改代码、资源或配置前，先说明作业逻辑，等用户同意。
5. 再进行代码、资源或配置修改。

## 长任务等待规则

1. 字体烘焙、Unity 导入、构建、下载等长任务，单次等待时间最多为 5 分钟。
2. 每等待 5 分钟，必须主动检查一次进程状态、日志进度和相关输出文件的更新时间，确认任务仍在运行还是已经完成、失败或卡住。
3. 检查后若任务仍正常运行，可以继续等待下一个 5 分钟；不得一次性设置十几分钟的阻塞等待。
4. 每次进度检查后，应向用户简短说明当前状态，例如正在处理哪一步、是否有新输出、是否需要继续等待。
5. 如果日志文件被占用或无法读取，应改用进程 CPU、窗口响应状态、输出文件大小和修改时间等方式判断，不能因为日志读取失败而持续盲等。

## TMP 字体渲染已知问题

### Unity 6.4 下 SDF 字体底部发虚

- 表现：TextMeshPro 的 SDF 字体会在字形下方出现灰色软化或阴影，放大文字后相对减弱。即使 `Face Dilate`、`Outline Softness`、`Underlay` 均为零或关闭，问题仍然存在。
- 影响范围：使用项目内 `Assets/字体包/Shaders/TMP_SDF-Mobile.shader` 的 SDF 字体。位图字体不受影响。
- 已排除：字体源文件损坏、Atlas 图集烘焙错误、字体采样模式、采样点大小、Padding、Perspective Filter、MipMap、纹理压缩、Outline 和 Underlay 配置。
- 验证结果：`SDFAA`、`SDF8`、`SDF16`、`SDF32` 模糊程度基本相同；导出的 Atlas Alpha 距离场正常且对称；改用屏幕导数计算后文字显示干净。
- 根因：原 TMP Mobile SDF Shader 在顶点阶段通过投影矩阵、屏幕尺寸和 `TEXCOORD1.y` 估算 SDF 屏幕缩放。在当前 Unity 6.4 的 UI/Canvas 缩放链路下，该估算产生误差，导致距离场边缘被错误扩宽。
- 正式修复：`Assets/字体包/Shaders/TMP_SDF-Mobile.shader` 已改为在像素阶段使用 `fwidth` 屏幕导数计算距离场缩放，并保留 Face、Outline 和 Underlay 功能。
- 后续约束：不要通过继续提高采样尺寸、增加 Padding、重新下载字体、开启 Underlay 或调整 Face 参数来掩盖此问题。若更新或重新导入 TMP Essential Resources，必须检查该 Shader 是否被覆盖，并重新验证中文 SDF 字体与描边效果。
