# Agents 必读规则

所有 agents 的所有 session 在开始任务前必须先阅读本文件。

## 协作规则

1. 不要做额外内容，不要做兜底。即使可能出现逻辑 bug，也不要自行扩展或补救；编码错误、编译错误、引用错误等可以修。保持逻辑干净利落，问题也干净利落地暴露。
2. 必须使用简体中文和用户沟通。新建或修改的说明文件、规则文件、文档文件也必须使用简体中文。撰写 git commit 信息时也必须使用简体中文。
3. Unity 中暴露到 Inspector 的参数名称、说明、菜单名等，应尽量做成中文，方便用户直接理解。
4. 用户提出需求时，不要立刻作业。必须先用简体中文说明准备怎么作业、会改哪里、为什么这样做；等待用户明确同意后，再开始执行。
5. 提到代码中的功能、类、方法、变量、组件、资源类型时，尽量翻译成中文解释。不要只给代码名字；需要说明它的中文含义和用途。

## 项目结构速览

这是一个 Unity 项目，项目根目录为 `C:\Users\neikuy\Desktop\Project-A`。

- `Assets/Scenes`：Unity 场景文件。
- `Assets/Scripts`：主要 C# 脚本。
- `Assets/Editor`：Unity 编辑器扩展脚本，只在编辑器中运行。
- `Assets/Resources`：通过 `Resources.Load` 等方式运行时加载的资源。
- `Assets/Shaders`：项目 Shader 文件。
- `Assets/资源`：主要美术、图标、贴图、场景素材等中文资源目录。
- `Assets/配音`：语音或音效相关资源。
- `Assets/音乐包`：音乐资源。
- `Assets/字体包`：字体资源。
- `Packages`：Unity 包管理配置。
- `ProjectSettings`：Unity 项目设置。
- `Library`、`Temp`、`obj`：Unity 或编译生成目录，通常不要手动修改。

## 作业前最小检查

1. 先读本文件。
2. 只围绕用户当前需求定位相关目录和文件。
3. 先说明作业逻辑，等用户同意。
4. 再进行代码、资源或配置修改。
